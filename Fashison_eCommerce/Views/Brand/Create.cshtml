@model Fashison_eCommerce.ViewModel.ProductViewModel

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@model Fashison_eCommerce.ViewModel.BrandViewModel
<h2> Create </h2>
<div align="center">
    @using (Html.BeginForm("Create", "Brand", FormMethod.Post))
    {
        <table class="table">
            @*<tr>
                <td>@Html.LabelFor(model => model.brand.BrandID) </td>
                <td>@Html.TextBoxFor(model => model.brand.BrandID) </td>
            </tr>*@
            <tr>
                <td>@Html.LabelFor(model => model.brand.BrandName) </td>
                <td>@Html.TextBoxFor(model => model.brand.BrandName) </td>
            </tr>
          
        </table>
    }
</div>


<div>
    @Html.ActionLink("Back to List", "Index")
</div>
